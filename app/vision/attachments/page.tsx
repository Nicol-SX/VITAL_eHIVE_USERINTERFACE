'use client';

import React from 'react';
import Attachment from '../components/Attachment';

export default function AttachmentPage() {
  return <Attachment defaultTab="Attachments" isOpen={true} onClose={() => {}} />;
} 